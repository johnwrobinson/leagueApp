<%= form_for(player_in_league) do |f| %>
  <% if player_in_league.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(player_in_league.errors.count, "error") %> prohibited this player_in_league from being saved:</h2>

      <ul>
      <% player_in_league.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_id %>
    <%= f.text_field :player_id %>
  </div>

  <div class="field">
    <%= f.label :league_id %>
    <%= f.text_field :league_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
