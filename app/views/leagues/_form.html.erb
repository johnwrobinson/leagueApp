<%= form_for(league) do |f| %>
  <% if league.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(league.errors.count, "error") %> prohibited this league from being saved:</h2>

      <ul>
      <% league.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :game_type_id %>
    <%= f.collection_select :game_type_id, GameType.all, :id, :name %>
  </div>

  <div class="field">
    <%= f.label :season %>
    <%= f.text_field :season %>
  </div>

  <div class="field">
    <%= f.label :number_of_players %>
    <%= f.number_field :number_of_players %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
